/*
 * Memento Lab
 *
 * This is the only file a practicum participant edits. The legacy Lab
 * tools in this image test it with btest and verify its rules with dlc.
 *
 * Integer puzzles may use only constants 0..255, arguments, local int
 * variables, and ! ~ & ^ | + << >>. Do not use control flow, macros, helper
 * functions, casts, or other data types. Floating-point puzzles may use
 * control flow and unsigned values, but may not use floating-point operations.
 */

/* bitAnd - x & y using only ~ and |. Max ops: 8. */
int bitAnd(int x, int y) {
    return 0;
}

/* getByte - extract byte n (0 is the least-significant byte). Max ops: 6. */
int getByte(int x, int n) {
    return 0;
}

/* logicalShift - shift x right by n, filling with zeroes. Max ops: 20. */
int logicalShift(int x, int n) {
    return 0;
}

/* bitCount - count the one bits in x. Max ops: 40. */
int bitCount(int x) {
    return 0;
}

/* bang - compute !x without using !. Max ops: 12. */
int bang(int x) {
    return 0;
}

/* tmin - return the minimum two's-complement integer. Max ops: 4. */
int tmin(void) {
    return 0;
}

/* fitsBits - whether x fits in an n-bit two's-complement value. Max ops: 15. */
int fitsBits(int x, int n) {
    return 0;
}

/* divpwr2 - compute x / 2^n, rounding toward zero. Max ops: 15. */
int divpwr2(int x, int n) {
    return 0;
}

/* negate - return -x. Max ops: 5. */
int negate(int x) {
    return 0;
}

/* isPositive - return 1 when x is positive, otherwise 0. Max ops: 8. */
int isPositive(int x) {
    return 0;
}

/* isLessOrEqual - return 1 when x <= y, otherwise 0. Max ops: 24. */
int isLessOrEqual(int x, int y) {
    return 0;
}

/* ilog2 - floor(log2(x)), for x > 0. Max ops: 90. */
int ilog2(int x) {
    return 0;
}

/* float_neg - return the bit representation of -f; preserve NaN. Max ops: 10. */
unsigned float_neg(unsigned uf) {
    return 0;
}

/* float_i2f - return the bit representation of (float)x. Max ops: 30. */
unsigned float_i2f(int x) {
    return 0;
}

/* float_twice - return the bit representation of 2*f; preserve NaN. Max ops: 30. */
unsigned float_twice(unsigned uf) {
    return 0;
}
